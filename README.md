# twh_aman_dbt

A dbt project scaffolded for Snowflake.

## Setup

1. Copy `profiles.yml.example` to `~/.dbt/profiles.yml`.
2. Fill in your Snowflake connection values.

## Run dbt

```bash
dbt debug
dbt run
dbt test
```
